import {describe,it,expect} from 'vitest';import {mask,parseSize} from '../src/core.js';describe('firebase-switcher-cli',()=>{it('parses sizes',()=>expect(parseSize('1MB')).toBe(1048576));it('masks secrets',()=>expect(mask('token=abc')).toContain('***'));});
